import queue
def reverseFirst(q,k):
    s=[]
    while(i<k):
        s.append(queue.queue[0])
        queue.get()
    while(len(Stack) != 0):  
        queue.put(s[-1])  
        s.pop() 
        
    
